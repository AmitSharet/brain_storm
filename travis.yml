language: python
python:
  - "3.8"
# command to install dependencies
install:
  - pip install -r requierments.txt
# command to run tests
script:
  - pytest --cov-report term --cov=sctp
after_success:
  - codecov